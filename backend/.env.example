# MongoDB Atlas Connection String (Required)
# Replace with your actual MongoDB Atlas URI. If your string does not include a database name,
# this app will connect to the `CodeIDE` database by default.
# Example (with DB name):
# mongodb+srv://username:password@cluster0.xrgk9it.mongodb.net/CodeIDE?retryWrites=true&w=majority
# Example (without DB name):
# mongodb+srv://username:password@cluster0.xrgk9it.mongodb.net/?retryWrites=true&w=majority
# Note: Special characters in password must be URL-encoded (@ = %40)
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/CodeIDE?retryWrites=true&w=majority

# Node Environment
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# JWT Secret (generate a random string)
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# Server Port (optional, defaults to 3000)
PORT=3000
